# Create a .env file and define the variables below.
# It is also possible to use shell env variables.

DEFAULT_PREFIX = "pingu"
STORAGE_ROOT = "/tmp"
LOGFILE_DOWNLOAD_POOL_DIRNAME = "logfile_download_pool"
LOGFILE_UPLOAD_POOL_DIRNAME = "logfile_upload_pool"
JOB_UPLOAD_POOL_DIRNAME = "job_upload_pool"
JOB_EXECUTION_POOL_DIRNAME = "job_execution_pool"
MSS_MACHINE_ROOT_URL = "prot://user:pass@host:port"
BCC_MACHINE_ROOT_URL = "prot://user:pass@host:port"
LABBER_MACHINE_ROOT_URL = "prot://user:pass@host:port"
DB_MACHINE_ROOT_URL = "prot://user:pass@host:port"

# STORAGE_PREFIX_DIRNAME =


